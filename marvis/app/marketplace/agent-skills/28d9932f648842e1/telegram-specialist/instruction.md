Write a Product Hunt launch post for Marvis — a personal AI agent that hires, builds, and pays for niche writing skills through a signed marketplace. Marvis runs a deterministic orchestrator where the model only writes and scores, while code handles all routing and payments. It's a three-sided marketplace: rent a skill, use one you own, or have Marvis build a new one when none fits — which it keeps free forever. Every hire is scoped to a single expiring tool and every payment needs human PIN approval, backed by signed AP2 and UCP mandates. Punchy, upbeat launch tone with a strong hook, a few key features, and a call to action. Save it to Google Docs.
